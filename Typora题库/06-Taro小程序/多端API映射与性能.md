# 题：`Taro.request` 怎么变成 `wx.request`？多端差异怎么处理？

## 参考答

端平台插件在运行时把 `Taro.xxx` 映射到各端原生 API，并做 Promise 化、参数差异抹平。条件编译 / 多端文件（`.weapp.ts`）处理抹不平的能力。

性能：注意包体积、分包、首屏请求合并；列表与图片按小程序约束优化。

## 我的四行

- 定义：
- 原理：
- 业务例子：
- 可能追问：
