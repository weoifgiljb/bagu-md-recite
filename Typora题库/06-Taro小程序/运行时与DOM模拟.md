# 题：Taro 3 如何让 React 跑在小程序上？

## 参考答

小程序没有浏览器 DOM。Taro 运行时在逻辑层模拟一套精简 DOM/BOM，再把变更同步到小程序视图层（类似桥）。React reconciler 照常工作，但最终宿主不是浏览器，而是小程序节点。

因此：部分 Web API 没有；CSS 支持有差异；不能假设 `window` 完整。

## 我的四行

- 定义：
- 原理：
- 业务例子：
- 可能追问：
